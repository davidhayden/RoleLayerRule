<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

    <PropertyGroup>
        <PackageId>RoleLayerRule.OrchardCore</PackageId>
        <Title>Role Layer Rule for Orchard Core</Title>
        <Description>Adds a role layer rule.</Description>
        <PackageTags>RoleLayerRule;OrchardCore</PackageTags>
        <VersionPrefix>1.0.0</VersionPrefix>
        <VersionSuffix>beta3</VersionSuffix>
    </PropertyGroup>

    <!-- Called after so that the <VersionSuffix> is built after the local ones -->
    <Import Project="Common.props"/>

</Project>